var eventHandler = require('../eventHandler');

describe('tests', function() {
  it('should set commit count, additions and deletions for an author after a push event containing a single commit', function() {});
  it('should set commit count, additions and deletions for an author after a push event containing multiple commits', function() {});
  it('should set commit count, additions and deletions for an author after a merge commit');
  it('should set commit count, additions and deletions for an author after a push event containing more than 20 commits', function() {});
  it('should increment pull requests for an author after a push event', function() {});
});